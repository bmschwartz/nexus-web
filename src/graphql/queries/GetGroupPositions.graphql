query GetGroupPosition($input: GroupInput!, $limit: Int, $offset: Int) {
  group(input: $input) {
    id
    positions(limit: $limit, offset: $offset) {
      totalCount
      positions {
        ...PositionDetails
      }
    }
  }
}