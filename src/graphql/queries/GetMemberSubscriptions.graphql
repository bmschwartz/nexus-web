query GetMemberSubscriptions($membershipInput: MembershipInput!) {
  myMembership(input: $membershipInput) {
    id
    orders(input: $ordersInput) {
      totalCount
      orders {
        ...OrderDetails
      }
    }
  }
}
