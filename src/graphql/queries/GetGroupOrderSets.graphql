query GetGroupOrderSets($input: GroupInput!) {
  group(input: $input) {
    orderSets {
      id
      symbol
      price
      side
      percent
      exchange
      orderType
      description
      createdAt
    }
  }
}